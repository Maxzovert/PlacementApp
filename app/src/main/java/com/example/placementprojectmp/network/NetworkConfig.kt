package com.example.placementprojectmp.network

object NetworkConfig {
    /** Spring default `server.port=8081`; use `http://10.0.2.2:8081/` for Android emulator → host loopback. */
    const val BASE_URL = "https://nonextensible-kiesha-unbreathed.ngrok-free.dev"
}